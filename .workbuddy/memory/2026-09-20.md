# 2026-09-20

- 完成 TC-cMLP 新论文的规划与初稿：
  - `论文内容/03_论文大纲.md`：章节大纲（I–IV）、图表规划（4 图 2 表）、标题候选、与前作 S-cMLP 的差异声明、待补充清单。
  - `论文内容/04_论文草稿_引言与方法.md`：英文 Introduction（5 段 + 4 条贡献）与 Method（A–E 五小节，公式 1–17），内容与 `01_TC-cMLP方法说明.md` 及 `Code/TC_cMLP/src/tc_cmlp/model.py`、`training.py` 的实际实现核对过（窗口专属第一层、共享层步长 η/W、group lasso proximal update、best-state early stopping、第一层同值初始化均来自代码）。
- 阅读材料：README.md、两份论文内容文档、前作 iCONECCT 2025 论文 PDF 全文（用 pypdf 提取到 `Result/_paper_outline_work/paper_text.txt`，前作结果 precision 88.9% / sensitivity 100% / F1 0.941，引用沿用其参考文献表）。
- 引用约定：[12] = Tank et al.；[13] = Yuan & Lin；[TBD] = 动态连接估计文献，终稿统一编排。
- 写作原则（用户明确）：新论文作为独立新工作撰写，正文不引用前作会议论文、不出现 S-cMLP 等前作方法名与前作实验数值；前作 PDF 仅作为写法参考。已按此原则修订大纲与英文底稿。
- 新增 `论文内容/05_论文草稿_实验_中文.md`：第 III 节（A 数据集与预处理 / B synthetic / C 网络演化 / D SOZ 定位 / E 消融），公式 (18)–(21)；数据集事实沿用前作核实的 ds004100 信息，全部实验数值以【待填】占位，待 `Result/` 输出后填写。
- 新增 `论文内容/07_论文初稿_中文.md`：整合全部章节（标题、摘要、I 引言、II 方法、III 实验、IV 结论、参考文献）为完整初稿供用户审阅；公式 (1)–(21) 连续编号；图 1–4 与表 I–II 以占位块插入对应位置，每个占位块说明图题、内容构成、数据来源与插入位置；文末附审阅待办清单。分文件（03–06）保留作为素材来源。
- 注意：HUP116 的 SOZ channel 具体名称在前作正文写作 "RA and RH"，草稿中以【待填】标注，定稿前需与数据标注核对。
- 写作流程调整：用户要求先出中文版论文内容，英文后续再补。已创建 `论文内容/04_论文草稿_引言与方法_中文.md`（中文主稿，公式编号与英文底稿一致），英文底稿保留在 `04_论文草稿_引言与方法.md` 待中文稿确认后修订。
- 完成 `07_论文初稿_中文.md` 的专业润色修订（原位覆盖）：摘要改为"挑战→方法→结果"结构，首句补入滑动窗口短窗样本稀缺的问题动机；引言第二段补入原稿已列但未引用的文献 [5][9][10][11]，第三段主题句改为网络持续重组；III-D 的 TP/FP/FN 定义改为基于预测集合与临床标注的集合表述；公式编号、占位块与【待填】标注全部保持原样。审阅清单新增：参考文献 [6]（Roebroeck et al.）条目信息不完整，终稿前补齐。
- 第二轮润色（用户要求以科研专家视角修订）：摘要重写，问题句明确"窗口长度与样本数量"的固有矛盾，iEEG 全称先于缩写出现；引言删除未定义的 GC 缩写、"生理内容"改"生理状态"等措辞修正；公式 (7)–(10) 窗口第一层参数记号统一为上标 $(1,w)$ 形式（$W_i^{(1,w)}$、$b_i^{(1,w)}$），(10) 后补充权重组与第一层张量的关系说明；II-E 节 SOZ 判定比例由 $K\%$ 改为直接写 18%，消除与最大 lag $K$ 的记号冲突；III-B 随机种子表述澄清为"相同的 10 个随机种子分别训练"；结论 "network change" 改为"网络变化"。素材分文件（04–06）未同步此轮记号修改，后续英文稿修订时需以 07 的记号为准。
- 新增 `picture/` 目录与论文四张示例图（`generate_example_figures.py` 生成，matplotlib 300 dpi，图 2–4 为模拟数据并在图内右下角标注 "Example figure — simulated data"）：fig1 方法框架示意（iEEG→滑动窗口→窗口专属第一层→共享层→causal matrix 序列→outflow→SOZ 排名，含三处正则化标注，矩阵记号用 $C^{(w)}$、第一层用 $W_i^{(1,w)}$ 与 07 初稿一致）；fig2 synthetic 恢复（真网络 A/B、两方法窗口 5 估计、$D^{(w)}$ 三方法曲线 + change point 竖线）；fig3 模拟 50 通道 iEEG 叠加波形（SOZ channel 20–28 红色）+ preictal/ictal 两个 50×50 causal matrix（SOZ 列红框）；fig4 三方法 156 窗口 outflow 时间曲线（ictal 灰底、SOZ 红、其他蓝）。环境注意：managed Python 3.13 无 matplotlib 且 envs/default venv 不存在，绘图用的是系统 Python312（matplotlib 3.11 + numpy 2.4）。真实实验出图后由 `Result/` pipeline 输出替换。
